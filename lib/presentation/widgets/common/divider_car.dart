import 'package:flutter/material.dart';

class DividerCar extends StatelessWidget {
  const DividerCar({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Divider(
      color: Colors.grey[700],
    );
  }
}
